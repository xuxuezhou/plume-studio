{
  "name": "plume-studio",
  "version": "0.4.0",
  "description": "Plume Studio — a web-based AI-assisted writing desk for WeChat Official Account publishing.",
  "author": "Allen",
  "main": "server.js",
  "private": true,
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "dev": "node --watch --env-file-if-exists=.env server.js",
    "check": "node --check server.js && node --check lib/store.js && node --check public/shared/markdown.js && node --check services/openaiClient.js && node --check services/wechatClient.js && node --check public/app.js && node --check public/backend.js",
    "service:install": "bash scripts/install-service.sh",
    "service:uninstall": "bash scripts/uninstall-service.sh"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
